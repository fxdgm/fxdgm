The Figures are stored in *.pdf format. 
These files are in the gitignore to reduce the storage.

The necessary data to reproduce the Figures is stored in `../Data` and all Figures can be reproduced with the scripts from ``../Visualizations``.