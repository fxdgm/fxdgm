# Maintenance

The actively maintained scripts are the `VisAll.ipynb` and the `VisConvergence.py`.
The remaining `.py` scripts are not actively maintained and just kept in this repo for users who not prefer jupyter notebooks.
However, no guarantee is given that those visualizations work, as they should.
